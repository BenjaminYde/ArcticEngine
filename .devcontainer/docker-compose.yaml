services:
  devcontainer:
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    user: developer
    volumes:
      # workspace
      - ..:/home/developer/ArcticEngine
      # host related
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /dev:/dev:rw
    environment:
      # host related
      - DISPLAY=$DISPLAY
      - XAUTHORITY=$XAUTHORITY
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    privileged: true
    network_mode: host